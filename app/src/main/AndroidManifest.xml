<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    package="com.example.android.metercollectionapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".MeterCollectionApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeterCollectionApp">
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.example.android.metercollectionapp.androidx-startup"
            tools:node="remove" />

    </application>

</manifest>

<!-- applicationId = имя пакета (com.example.android.metercollectionapp) -->
<!-- TODO: выяснить почему инициализация (см. ниже) не приводила к подгрузке кастомной конфигурации WorkManager'а -->
<!--<provider-->
<!--android:name="androidx.startup.InitializationProvider"-->
<!--android:authorities="com.example.android.metercollectionapp.androidx-startup"-->
<!--android:exported="false"-->
<!--tools:node="merge">-->
<!--&lt;!&ndash; If you are using androidx.startup to initialize other components &ndash;&gt;-->
<!--<meta-data-->
<!--    android:name="androidx.work.impl.WorkManagerInitializer"-->
<!--    android:value="androidx.startup"-->
<!--    tools:node="remove" />-->
<!--</provider>-->